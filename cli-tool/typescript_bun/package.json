{
  "name": "server",
  "module": "src/index.ts",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run dist/index.js",
    "build:windows": "bun build ./src/index.ts --compile --minify --sourcemap --target=bun-windows-x64 --outfile dist/codingchallenge"
  },
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
